apiVersion: v1
kind: Service
metadata:
  name: {{ include "datalake.fullname" . }}-trino-coordinator
  labels:
    app.kubernetes.io/component: trino-coordinator
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.trino.coordinator.ports.http }}
      targetPort: http
  selector:
    app.kubernetes.io/component: trino-coordinator
